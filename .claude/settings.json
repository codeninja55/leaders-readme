{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); [ -n \"$f\" ] || exit 0; case \"$f\" in */NOTES.md|NOTES.md) exit 0;; *.md) markdownlint-cli2 \"$f\" || exit 2;; esac",
            "timeout": 30,
            "statusMessage": "markdownlint"
          }
        ]
      }
    ]
  }
}
